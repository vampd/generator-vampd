language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-vampd' ]; then cd .. && eval "mv $currentfolder generator-vampd" && cd generator-vampd; fi

